// NOTE:
// This spec was previously a stub to avoid running duplicate tests after moving.
// For reporting parity (and to avoid `testCount: 0`), we forward to the canonical
// spec that holds the real tests.
//
// This does NOT add new coverage—it's only an alias so tooling that scans
// `cypress/e2e/api/*.cy.ts` sees the health tests.
import './public/06.openapi.health-public.cy';
